FileCopierWithCamel:

  Run below command to instructs Maven to compile the source in the src directory and to execute
  the FileCopierWithCamel class with the camel-core JAR on the classpath.
    mvn compile exec:java

