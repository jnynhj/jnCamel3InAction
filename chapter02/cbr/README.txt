OrderRouterTest:
  * Implement cbr and filter TCs in chapter2
    - CBR EIP
    - Filter EIP
    - Multicast and parallelProcessing

  * To run
        mvn test -Dtest=OrderRouterTest

To run all tests:
    mvn test