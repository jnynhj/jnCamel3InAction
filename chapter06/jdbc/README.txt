JdbcTest: P221
  *  6.5.1 Accessing data with the JDBC component
  *  Component: jdbc, jms
  *  DSL: from(), to()
  *  Annotation: N/A
  *  Dependency: camel-core, derby, spring-jdbc, camel-jdbc
  *  Run: mvn test -Dtest=JdbcTest


To run:
    mvn camel:run
To run a test class:
        mvn test -Dtest=<testClassName>

To run all tests:
    mvn test