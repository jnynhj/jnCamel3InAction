PurchaseOrderConverterTest: P104
  *  Type converter by writing your own type converter
     - Write PurchaseOrderConverter class
     - compile and copy generated/camelinaction/PurchaseOrderConverterLoader.java to main/java/camelinaction
     - Run mvn test -Dtest=PurchaseOrderConverterTest

  *  DSL: convertBodyTo()

To run a test class:
        mvn test -Dtest=<testClassName>

To run all tests:
    mvn test